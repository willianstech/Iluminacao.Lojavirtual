@model Iluminacao.Lojavirtual.Dominio.Entidade.Carrinho

<div class="navbar-right">
    @Html.ActionLink("Carrinho","Index", "Carrinho", new {returnUrl = Request.Url.PathAndQuery}, new {@class="btn btn-default navbar-btn"})
</div>

<div class="navbar-right navbar-text">
    <b>Seus produtos:</b>
    @Model.ItensCarrinho.Sum(x => x.Quantidade) item(s), @Model.ObterValorTotal().ToString("c")
</div>



